package lk.sms.repository;

import lk.sms.entity.LocalMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LocalMapRepository extends JpaRepository<LocalMap, Long> {
}
